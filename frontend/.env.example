# Base URL for the backend API
# For local development, leave blank or set to http://localhost:5000 (api.js falls back to localhost:5000 automatically)
# For production deployment (e.g. Vercel pointing to Render/Railway backend):
# VITE_API_URL=https://your-backend-api.onrender.com
VITE_API_URL=http://localhost:5000
