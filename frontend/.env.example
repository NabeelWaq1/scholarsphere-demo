# Base URL for the backend API
# For local development, leave blank or point to http://localhost:5000 (api.js falls back to http://localhost:5000 automatically)
# For production deployment on Vercel, set this to your deployed Render/Railway backend URL:
# VITE_API_URL=https://your-backend-api.onrender.com
VITE_API_URL=
